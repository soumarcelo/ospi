{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // Seção para configuração do Kafka
  "KafkaConfig": {
    "BootstrapServers": "localhost:9092" // Valor padrão, será sobrescrito
  },

  // Seção para configuração do PostgreSQL
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=ospi_engine_db;Username=engine_user;Password=your_strong_password" // Valor padrão, será sobrescrito
  }
}